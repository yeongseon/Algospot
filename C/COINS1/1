#include <stdio.h>
#include <stdlib.h>

typedef struct node_s node_t;
typedef struct list_s list_t;

struct node_s {
  int datalen;
  int *dp;
  node_t *next;
};

struct list_s {
  int size;
  node_t *head;
  node_t *tail;
};

list_t* list() { 
  list_t *l;
  l = (list_t *) malloc (sizeof(list_t));
  l->size = 0;
  l->head = NULL;
  l->tail = NULL;
  return l;
}

int appendtail
int main() {
  int tc = 0; /* test case */
  int i = 0;

  scanf("%d", &tc);

  for (i = 0; i < tc ; i++ ) {
  }

  return 0;
}
