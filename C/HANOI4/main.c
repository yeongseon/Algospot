#include <stdio.h>
#include <stdlib.h>

int main() {
  int i, j = 0;
  int tc = 0; /* test case */
  int n = 0;

  scanf("%d", &tc);

  for (i = 0; i < tc ; i++ ) {

    scanf("%d", &n);

  }

  return 0;
}
